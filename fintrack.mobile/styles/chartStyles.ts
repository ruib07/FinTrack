import { StyleSheet } from "react-native";

export default StyleSheet.create({
  legendContainer: {
    justifyContent: "center",
    flexDirection: "row",
    marginTop: 10,
  },
  legendItem: {
    flexDirection: "row",
    alignItems: "center",
    marginRight: 15,
  },
  colorBox: {
    width: 20,
    height: 20,
    marginRight: 5,
  },
  legendText: {
    fontSize: 16,
  },
});
